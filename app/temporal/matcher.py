def match_boxes(prev_boxes, curr_boxes):
    """
    Greedy IoU / center-distance matching.
    Returns pairs: (prev, curr)
    """
    pairs = []
    # simple implementation for MVP
    return pairs
